import React from 'react'
import Register from './register/page'

export const metadata = {
  title: "Join NextPost",
};

const page = () => {
  return (
    <div><Register /></div>
  )
}

export default page